var users = ["Owais", "Areeb", "Bassam", "Haseeb"];
if (users.length == 0) {
    console.log("we need to find some users");
}
else {
    console.log("the list of users isn't empty");
}
console.log("__________________________________________");
users = [];
if (users.length == 0) {
    console.log("we need to find some users");
}
else {
    console.log("the list of users isn't empty");
}
